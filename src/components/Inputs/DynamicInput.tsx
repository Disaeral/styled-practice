import styled from 'styled-components';

export const DynamicInput =  styled.input`
    border: 1px solid #333;
    height: 3rem;
    font-size: 1.25rem;
    border-radius: .25rem;
    padding: 0 .5rem;
    &:focus-visible {
        outline: none;
    }
`